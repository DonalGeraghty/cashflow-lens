# Copy to .env and fill in to enable "Load from Google Sheets".
# This is an OAuth 2.0 *Client ID* (type: Web application), not a secret,
# but .env is git-ignored anyway. See README → Google Sheets for the setup.
VITE_GOOGLE_CLIENT_ID=
